# This file contains the credentials for the voice & chat platforms
# which our bot is using.
# https://rasa.com/docs/rasa/messaging-and-voice-channels

# Enable REST channel (for testing via Postman, curl, or webchat)
rest:
  # nothing needed here

# SocketIO channel (for connecting to web chat widget)
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# Example configs for other channels (commented out until we need them)
# facebook:
#   verify: "<verify>"
#   secret: "<our secret>"
#   page-access-token: "<our page access token>"

# slack:
#   slack_token: "<our slack token>"
#   slack_channel: "<the slack channel>"
#   slack_signing_secret: "<our slack signing secret>"

# mattermost:
#   url: "https://<mattermost instance>/api/v4"
#   token: "<bot token>"
#   webhook_url: "<callback URL>"











